"""Learnix backend modules.

The legacy prototype still exposes routes from backend/app.py. New platform
features are registered as blueprints here so the monolith can be reduced
progressively without breaking existing endpoints.
"""

